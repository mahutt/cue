<get-link action="<%= deck.position %>" target="#content" class="deck"
    ><%= deck.name %><% if (deck.percentage !== undefined && deck.percentage !== null) { %> (<%= deck.percentage %>%) <%
    } %></get-link
><br />
